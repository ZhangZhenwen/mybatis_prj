<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhenwen.dao.UserDao">

    <!-- 使用resultMap时，要将sql语句中的resultType改成resultMap -->
    <resultMap id="userMap" type="com.zhenwen.domain.User">
        <id  column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="address" property="address"/>
        <result column="sex" property="sex"/>
        <result column="birthday" property="birthday"/>
    </resultMap>

    <!-- 配置查询所有 -->
    <select id="findAll" resultType="com.zhenwen.domain.User">
        select *
        from user
    </select>

    <!-- 保存用户 -->
    <insert id="saveUser" parameterType="com.zhenwen.domain.User">
        <selectKey keyProperty="id" keyColumn="id" resultType="int" order="AFTER">
            select last_insert_id()
        </selectKey>
        insert into user(username, birthday, sex, address)
        values (#{username}, #{birthday}, #{sex}, #{address})
    </insert>

    <update id="updateUser" parameterType="com.zhenwen.domain.User">
        update user
        set username=#{username},
            birthday=#{birthday},
            sex=#{sex},
            address=#{address}
        where id = #{id}
    </update>

    <delete id="deleteUser" parameterType="Integer">
        delete
        from user
        where id = #{id}
    </delete>

    <select id="findById" parameterType="Integer" resultType="com.zhenwen.domain.User">
        select *
        from user
        where id = #{id}
    </select>

    <select id="findByName" parameterType="String" resultType="com.zhenwen.domain.User">
        select *
        from user
        where username like #{name}

        #       不推荐
#       另一种写法  ${value} value是固定的 不能改成其他名称
#       采用的是字符串拼接的方式，存在sql注入的风险
#         select *
#         from user
#         where username like '%${value}%'
    </select>

    <select id="findTotal" resultType="Integer">
        select count(id)
        from user
    </select>

    <select id="findUserByVo" parameterType="com.zhenwen.domain.QueryVo" resultType="com.zhenwen.domain.User">
        select *
        from user
        where username like #{user.username}
    </select>
</mapper>


